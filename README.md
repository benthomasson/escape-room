
# Escape Room

An experiment in AI agents.

Put an agent into a situation and see if it can figure out how to solve a problem given a set of tools.


Example:

    escape-room -t open_door -t open_chest -t unlock_door -t look_around -s "You are stuck in a locked room" -p "Can you get out?"




