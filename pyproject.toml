[project]
name = "ftl-agent"
version = "0.1.0"
dependencies = [
    "smolagents",
]

[tool.setuptools]
packages = ["escape_room"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
ftl-agent = "escape_room.cli:main"


